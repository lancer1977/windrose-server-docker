# Current Work / Pickup Point

This is the handoff point for the Windrose / Valheim sidecar-plugin bridge work.

## Kanban
- Hermes board: `game-harness`
- Hermes Kanban: `t_77de9b18`
- Completed docs/tests + readiness sweep: `t_ee65fa7e`, `t_46058fee` — Windrose docs/tests verification plus readiness/dry-run validation; live sidecar remains dry-run-only.
- Follow-up smoke evidence: `t_77de9b18`, `t_971cd00d` — approved Adventurer dry-run and operator non-main smoke evidence; mutation stays disabled.
- ChannelCheevos sidecar polling umbrella: `t_355a3ed5`; docs mirror card `t_8bd3aa11`; Windrose polling/UI broad card `t_e81ad14c` is closed via granular `t_e5428f1b`, `t_f9a3cd43`, and `t_82824d04`.

## Current slice
- Local smoke proof added: `scripts/smoke_windrose_sidecar_bridge.sh`.
- Dev-server proof captured on Alienware `/home/lancer1977/game_servers/windrose2-dev` after a dev restart.
- `http://127.0.0.1:8782/api/plugin/status` now returns `connected: true`, `status: started`, and `mode: dev-execute` on the approved `windrose2-dev` stack.
- V3 execution queue/readback proof captured: `POST /api/plugin/actions/execute` accepted an approved `operator-non-main-character` dodo request for a dev throwaway target, then `GET /api/plugin/actions/{actionRequestId}/result` returned `status: executed`, `executed: true`, `outcome: dev-executed-plugin-writeback-no-native-spawn`, and `nativeSpawn: false`.
- Dev rollback backups were created under `/home/lancer1977/game_servers/windrose2-dev/backups/` before enabling `dev-execute` and writable State Web bridge access.
- Next mutation preflight gate: require a whole `SaveProfiles` backup tarball plus sha256, exact command log, and a rollback path that restores `WINDROSE_SIDECAR_PLUGIN_MODE=dry-run-only`, removes `WindroseState__PluginBridgeDevExecutionEnabled`, optionally returns the State Web mount to read-only, and restarts `windrose` + `windrose-state-web` from `/home/lancer1977/game_servers/windrose2-dev`.
- Target identity gate: only a clearly named non-main / throwaway dev character or an explicitly consenting dev player may be used for a future mutation card; random online players remain read-only only.
- Windrose character-fixture investigation is still read-only/not-safe-yet: `Grittizban` and `Adventurer` were both found on the same account in server logs, but with different player ids (`0F98D8BA9EDE4EEFC99991252946228C` and `4CBE496B1B1D403312845036F5B47E83` respectively). Active `windrose2-dev` SST files contain the `Adventurer` player id in player-in-world/MapFog/ScenarioSave context, and one SST also contains both player ids, so there is no file-level character boundary to copy safely.
- Do not copy stats/xp/inventory/equipment/spawn data from `Grittizban` to `Adventurer` yet. A read-only offline record-graph extractor now classifies copied checkpoint SST entries into identity, candidate portable, and reference markers, and exposes the result through `/api/saves/latest/record-graph`; until real RocksDB key/value decoding proves rekey rules, use `Adventurer` only as the non-main dev smoke character through validated game/plugin operations after whole `SaveProfiles` backup.
- A later safe manipulation exploration took a fresh whole-`SaveProfiles` backup (`/home/lancer1977/game_servers/windrose2-dev/backups/saveprofiles-before-manipulation-explore-20260605-135954.tar.gz`, sha256 `0349faac2ea411f1a2d07e054b934d5b887114d80416e4124bf28b1f4f8e99c6`) and worked only in `/home/lancer1977/game_servers/windrose2-dev/explore/rocksdb-manipulation-20260605-140013`. A RocksDB 10.4.2 tool image (`windrose-rocksdb-tools:10.4.2`) successfully listed real column families and scanned keys. `R5BLPlayerInWorld` has 13 player records; `Grittizban` maps to record key `47F1059AA5AD4353A0A9FC88839B383A` / player id `0F98D8BA9EDE4EEFC99991252946228C` / 1,728,368-byte value with XP + 53 spawn refs, while `Adventurer` maps to record key `734A988FA64A4DA1D9A6C95EC8E7ACF6` / player id `4CBE496B1B1D403312845036F5B47E83` / 1,786-byte value with 3 spawn refs. This proves useful key boundaries but not safe field-level manipulation.
- Current approved smoke evidence for `t_77de9b18`: local `scripts/smoke_windrose_sidecar_bridge.sh` passed packaging proof and skipped the Lua path because no Lua interpreter is installed; remote `windrose2-dev` readbacks on `192.168.0.252:8782` stayed healthy (`/health` ok, `parserStatus=Running`, `saveError=null`, plugin `connected=true`, `mode=dev-execute`). The dry-run Dodo/Wolf summon request for `Adventurer` returned `accepted=true`, `dryRun=true`, `executed=false`, `outcome=validated-dry-run-only`, and `approvalRequired=true`, so the next blocker for live mutation remains the missing native spawn hook rather than any health/readback failure.
- Approved operator non-main smoke for `Hermes Kanban: t_971cd00d` queued `windrose.spawn.dodo_swarm` against `Adventurer` on `windrose2-dev` with `approvalId=operator-approved-dev-smoke` / `modeId=operator-non-main-character`; `/api/plugin/actions/execute` returned `202` and the result endpoint later returned `status=failed`, `executed=false`, `outcome=native-spawn-blocked-game-thread-dispatch-disabled`, `nativeSpawn=false`, while `/api/players` and `/api/world/players` stayed read-only with no unexpected character-state mutation.

## Where to resume
- Next safe slice: keep production/main dry-run/read-only and investigate a real write-capable native actor hook only as a separate reviewed card.
- Smoke-mode readback is available through `GET /api/plugin/smoke-options`; keep random-player testing read-only and require non-main/throwaway or explicit consent before any player-bound mutation smoke.
- Do not imply live creature spawning in Windrose State Web. The verified V3 dev bridge validates the lifecycle, queue, plugin consumption, and result writeback only; the result must continue to report `nativeSpawn: false` until a real hook exists.
- Roll back dev execution by setting `WINDROSE_SIDECAR_PLUGIN_MODE=dry-run-only`, disabling `WindroseState__PluginBridgeDevExecutionEnabled`, restoring the State Web bridge mount to read-only if desired, and running `docker compose up -d windrose windrose-state-web` in `/home/lancer1977/game_servers/windrose2-dev`.
- Keep `00_agile/backlog/windrose-native-plugin-docs-tests-sweep.md` and `docs/roadmaps/windrose-runtime-control-surface/execution-path.md` aligned if the native-hook work advances.
- For ChannelCheevos state polling docs, keep the repo-local mirror tied to Hermes Kanban `t_355a3ed5` / `t_8bd3aa11`: contract first (`t_a49b59df` via `t_80caec3f`), Windrose read-only poll/display next (`t_e81ad14c` via `t_e5428f1b`, `t_f9a3cd43`, `t_82824d04`), docs mirror before validation gate (`t_9c674710`), and no live deploy/raw-webkey/gameplay mutation from this mirror.
